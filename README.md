请使用hatch-pet skill生成标灰色龙猫桌宠，名字叫Olivia
必须使用我上传的 Olivia 照片作为同一只动物的身份参考，并在所有动作中保持一致：
- 圆润紧凑的体型
- 银灰色整体毛色
- 黑亮的大眼睛
- 宽大的直立耳朵
- 浅灰白色口鼻和胸口
- 连续、柔软、圆润的脸颊和胸口绒毛轮廓
- 细长胡须
- 温和、可爱、略带慵懒的表情
- 额头只保留非常浅的银灰色渐层
- 不要深色帽子、深色三角额头区域或明显黑色额纹
这次不要使用同一张静态图进行平移、缩放、旋转、镜像、亮度变化或简单局部涂鸦来冒充动作。每组动作必须重新绘制出真实、清晰、可辨认的身体姿态变化，包括身体重心、头部、耳朵、前爪、后脚、眼睛和尾巴的变化。
请制作以下 9 组动作：
1. idle，6 帧
         Olivia 安静坐着，脚掌稳定着地，身体有轻微呼吸起伏，耳朵轻微摆动，偶尔眨眼。动作幅度小，但 6      帧之间要能看出连续呼吸和眨眼变化。第一帧与最后一帧接近，形成平滑循环。
2. running-right，8 帧
        // Fill in ur own desc
3. running-left，8 帧
        // Fill in ur own desc
4. waving，4 帧
        // Fill in ur own desc
5. jumping，5 帧
        // Fill in ur own desc
6. failed，8 帧
        // Fill in ur own desc
7. waiting，6 帧
        // Fill in ur own desc
8. running，6 帧
        // Fill in ur own desc
9. review，6 帧
        // Fill in ur own desc

动作制作要求：
- 每一组都必须是真实重新绘制或可靠的姿态级生成；
- 同一组内部要有连续的动作节奏；
- 不同组之间必须一眼可区分；
- 保持 Olivia 的脸部、毛色、耳朵比例和圆润身体一致；
- 不要使用装饰性花瓣状、蒜瓣状、球状、分段状或尖刺状毛束；
- 每个动作帧都要适合缩小到 192×208 像素；
- 每组生成后，先输出该组的接触表和动作预览，让我确认动作语义，再继续下一组；
- 不要在没有展示接触表和预览的情况下直接组装最终 spritesheet。
请按以下阶段执行：
阶段 1：先生成 9 组动作的姿态参考图，每组至少展示 3 个关键姿态。
 阶段 2：我确认姿态差异后，再生成完整帧数。
 阶段 3：生成每一组的接触表和 GIF 动作预览。
 阶段 4：通过动作语义检查后，再组装 1536×2288 的 v2 spritesheet。
 阶段 5：生成 pet.json，并验证尺寸、透明背景、未使用单元格、动作行数量和方向语义。
 阶段 6：将最终文件保存到本地目录
